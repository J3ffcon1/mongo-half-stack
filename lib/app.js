// const { parse } = require('url');
// const { createReadStream } = require('fs');

// module.exports = (req, res) => {
//     if(req.url === '/' && req.method === 'GET'){
//         return createReadStream('_dirname/index.html') //creating readstream at current directory name.
//             .pipe(res);
//     } 

// };
